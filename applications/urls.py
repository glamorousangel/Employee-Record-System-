from django.urls import path
from . import views

# Ensure 'urlpatterns' is spelled correctly (all lowercase)
urlpatterns = [
    # If you have a view ready, you can add it here. 
    # If not, leave the list empty for now.
    # path('info/', views.application_info, name='application_info'),
]